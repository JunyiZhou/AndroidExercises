<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.songyang.healthmanager">

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HealthManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.view.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".info.view.InfoActivity" />
        <activity android:name=".login.view.LoginActivity" />
        <activity
            android:name=".rostrum.view.RostrumDetailActivity"
            android:label="@string/title_activity_rostrum_detail" />
        <activity
            android:name=".message.MessageActivity"
            android:label="@string/title_activity_message" />
        <activity
            android:name=".treat.view.DepartmentDetailActivity"
            android:label="@string/title_activity_department_detail" />
        <activity
            android:name=".treat.view.TreatDetailActivity"
            android:label="@string/title_activity_treat_detail"/>
    </application>

</manifest>
